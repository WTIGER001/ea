/*
File Upload Service
------------------------------------
Handles the upload of local files and converts them into the datasets
*/
'use strict';

angular
  .module('eaApp')
  .service('SessionService', ['$http', '$uibModalInstance', 'SessionService', 'lodash', 
  	function ($http, $uibModalInstance, SessionService, lodash) {







}])
